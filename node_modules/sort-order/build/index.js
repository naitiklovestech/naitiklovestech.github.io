"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function collectFirst(_a, fn) {
    var head = _a[0], tail = _a.slice(1);
    var result = head && fn(head);
    return result === undefined && tail.length ? collectFirst(tail, fn) : result;
}
function nonZero(a, b) {
    return function (compare) {
        var result = compare(a, b);
        return result === 0 ? undefined : result;
    };
}
function sortOrder() {
    var fns = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        fns[_i] = arguments[_i];
    }
    return function comparator(a, b) {
        return collectFirst(fns, nonZero(a, b)) || 0;
    };
}
exports.default = sortOrder;
